# Cross-repository defect ledger

| ID | Owner | Exact SHA | Evidence | Status |
| --- | --- | --- | --- | --- |
| CLI-INV-001 | Kernel DSL | `29610d232eb5df0ffd5c9d70dc70323577f9ec0d` | Generic `ResolvedDomainOf` lost `Domain`; compile-time Shell reproduction fixed by `46f306bc3` in kernel PR #387. | upstream PR open |
| CLI-INV-002 | Kernel Client | `29610d232eb5df0ffd5c9d70dc70323577f9ec0d` | `reference(domain, callable)` projected a dynamically loaded callable input to `never`; compile-time `ClientSession.invoke` reproduction fixed by `a80568d60` in kernel PR #387. Client typecheck and 25 files/141 tests pass. | upstream PR open |
| CLI-INV-003 | SDK Application | `a210e3c12c8a4b11d19c0651b870e77b2ff19fef` | Generic Schema plus exact `Runtime<Schema>` failed `defineApplication` because a redundant equality conditional did not reduce; fixed through the Runtime Schema witness by `e1ac657` in SDK PR #146. SDK typecheck and 76 files/444 tests plus policy gates pass. | upstream PR open |
| CLI-INV-004 | SDK / Kernel DSL | Kernel PR #394 commit `a693db320` | Exact `ResolvedMethod` owner and Client receiver satisfaction now preserve the selected Class plus its exact parent closure; unrelated receivers fail compile-time admission without an SDK or Shell shadow type. | fixed upstream |
| CLI-INV-005 | CLI Studio | `53ce495f23b458e245cd82e41919ecdce5f8dc57` | Frontend anatomy recognized removed `frontendArtifact`/`reactFrontend` forms but not SDK V1 `defineFrontend`; exact Vite/external/default-route tests reproduced the missing View metadata. Replaced with the V1 parser in this migration. | fixed locally |
| CLI-INV-006 | CLI Studio | `53ce495f23b458e245cd82e41919ecdce5f8dc57` | Studio duplicated canonical callable `input`/`output` into legacy `params`/`returns` and reconstructed obsolete handler authorization hooks. A stale projection could therefore satisfy the cache boundary without the canonical callable contract. Removed the shadow fields and added decoder rejection tests. | fixed locally |
| CLI-INV-007 | CLI Studio tests | `53ce495f23b458e245cd82e41919ecdce5f8dc57` | The Claude cancellation test waited for `pids.json` existence and could parse a partial write, reproduced in the full suite as `Unexpected EOF`. It now waits for a complete integer PID pair before asserting process-group termination. | fixed locally |
| CLI-INV-008 | CLI delivery workflows | CLI PR #128 and cleanup branch | The migration lockfile linked unpublished sibling sources and required a cross-repository credential. That temporary adapter is now retired: workflows install only ordinary packages and the boundary gate rejects links, workspaces, vendored Astrale archives, and `.cohort` topology. | closed; published SDK beta.17 and Shell 0.4.1 full package qualification passes |
| CLI-INV-009 | CLI status presentation | `de92b39d5` | A bookmark-scoped default identity is the effective credential owner for Kernel commands, while `identity whoami` intentionally reports the independent global identity-store default. Live status showed the bookmark identity and no global default; calls correctly selected Root automatically. This is two explicit scopes, not an authentication defect. The ledger records the distinction rather than merging stores or adding another identity API. | not a defect |
| CLI-INV-010 | SDK package publication | CLI cleanup branch against SDK beta.15 | The ordinary registry graph passed package and source-boundary admission but CLI typecheck could not resolve facade exports already merged on SDK main. The temporary source adapter previously hid that released beta.15 predated those exports. | closed by published SDK beta.17 and the full ordinary package gate; no CLI shadow types or source fallback |
| CLI-INV-011 | Shell package publication | CLI cleanup branch against SDK beta.16 and Shell beta.3 | Exact Bun 1.4.0 tests and build resolved SDK beta.16 successfully, then published `@astrale-os/shell@0.3.8-beta.3` imported removed SDK paths (`/domain`, `/schema/kernel`, and `/graph/model`). | closed by published Shell 0.4.1, ordinary lock refresh, and the full CLI package gate; no source fallback |
| CLI-INV-012 | CLI Studio browser fixture | `ded5efe33d9e8c91fa5a6e35ab95523f4db65864` | The SDK V1 migration removed the fixture's properties and Core node but retained browser assertions for both, so the real Chromium gate failed while the class-only projection rendered correctly. The fixture now authors the same property and Core evidence through current SDK V1 `valueSchema`, `property`, and `core.node` APIs; the smoke passes without weakening its assertions. | fixed locally; exact packed Shell plus CLI typecheck, full tests, build, and browser smoke pass |
| CLI-INV-013 | SDK value facade | SDK PR #157 / CLI `3b88948` | CLI's last Core import was canonical DNS-label admission. Copying its regular expression would create a shadow rule, while the SDK exposed no semantic path for the Core owner. `@astrale-os/sdk/value` now identity-reexports the Core predicate, assertion, schema, and expression; CLI consumes only that facade. | closed through published SDK beta.17 and the ordinary CLI package proof |
| CLI-INV-014 | CLI dependency qualification | CLI `3b88948` | The Studio source census used a recursive ts-morph glob. An exact linked SDK/adapter graph reproduced `ENAMETOOLONG` by following package dependency cycles despite the intended node_modules exclusion. The check now parses only Git-tracked Studio sources with dependency resolution disabled and retains the same all-string-literal rejection. | fixed locally; public dependency check green |
| CLI-INV-015 | CLI package publication | CLI `deb3b26d` | The public npm package was self-contained, but its workflow still requested a repository package token and `packages: write` solely for an optional GitHub Packages mirror. That retained unnecessary credential and registry ownership after source-cohort removal. | fixed locally; pinned npm-OIDC publisher, no mirror/token/package permission, 7/7 workflow contracts and complete package gate green |
| CLI-INV-016 | CLI failure diagnostics | `651d4d1a` / Lab run `run_4284844d743449f5a01613ce995224f3` | Published CLI beta.7 reached the real managed Admin through bare `astrale`, but `instance create --json --debug` emitted only the safe `UNEXPECTED_ERROR` envelope because commands using the shared `fatal()` path ignored explicit debug evidence. The safe ordinary diagnostic remains unchanged; one generic debug owner is now shared by Kernel and local command failures and exposes cause/Aggregate evidence only under `--debug`. | fixed locally; focused 7-test failure-safety proof, full typecheck, lint, and format green |
| CLI-INV-017 | SDK package compatibility | SDK beta.24 | Fresh CLI installation resolves SDK-owned Core beta.9/DSL beta.7 beside Client-owned Core beta.10/DSL beta.8, so strict typecheck rejects identical branded Schema, Path, Query, and Mutation values. CLI has no direct Kernel dependency and adding one would violate the facade boundary. | fixed in the exact packed SDK candidate by one SDK-owned beta.11/beta.10/beta.8/beta.10/beta.11 cohort; candidate CLI strict typecheck proves one Core and DSL version. Publication pending |
| CLI-INV-018 | CLI Admin identity selection | CLI `1ab899dc` | Instance creation persisted its selected WorkOS identity, but managed `instance use` without `--as` and setup adoption discarded the exact Admin caller and left later calls ambient-default-dependent. | fixed locally by binding one non-secret identity label at Admin session creation and carrying that exact label with caller-visible inventory into bookmark persistence; raw credentials remain unnamed and unstored |
| CLI-INV-019 | CLI usage diagnostics | CLI complete candidate suite | `renderCommanderError(program, error, argv)` accepted exact invocation arguments but chose machine output from mutable process-wide state; concurrent tests changing stdout mode made four suggestion tests fail while their focused suite passed. | fixed locally by deriving the production default from supplied invocation argv and stdout, while allowing the already-known output-mode decision to be passed explicitly; deterministic consumers no longer reread ambient state |

New entries require source SHA, reproduction, expected/actual behavior, impact, owner decision,
focused proof, and full qualification.
