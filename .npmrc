registry=https://registry.npmjs.org/
@jsr:registry=https://npm.jsr.io
# The CLI's @astrale-os dependencies are all on public npm, so resolve the scope
# there — no GitHub Packages token is needed in CI. In the umbrella workspace
# these packages are linked from source.
@astrale-os:registry=https://registry.npmjs.org/
