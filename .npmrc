@jsr:registry=https://npm.jsr.io
# The CLI's @astrale-os deps (kernel-client, kernel-core, ox) are all on public npm,
# so resolve the scope there — no GitHub Packages token needed in CI (matches the
# sdk/shell convention). In the umbrella workspace these are linked from source.
@astrale-os:registry=https://registry.npmjs.org
