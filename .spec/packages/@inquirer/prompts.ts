import { definePackage } from '@astrale-os/spec/authoring'

export default definePackage({
  package: '@inquirer/prompts',
  purpose: 'Provides bounded interactive prompts for human-operated CLI journeys.',
})
