import { definePackage } from '@astrale-os/spec/authoring'

export default definePackage({
  package: 'chokidar',
  purpose: 'Watches local Domain and viewer inputs during explicitly requested development flows.',
})
