import { definePackage } from '@astrale-os/spec/authoring'

export default definePackage({
  package: 'jose',
  purpose: 'Imports, verifies, encrypts, and signs CLI identity credentials and key material.',
})
