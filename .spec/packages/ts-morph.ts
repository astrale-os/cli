import { definePackage } from '@astrale-os/spec/authoring'

export default definePackage({
  package: 'ts-morph',
  purpose: 'Analyzes local TypeScript Domain sources for CLI development guidance.',
})
