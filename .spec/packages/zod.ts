import { definePackage } from '@astrale-os/spec/authoring'

export default definePackage({
  package: 'zod',
  purpose: 'Admits persisted CLI state, identity transfers, and external configuration values.',
})
