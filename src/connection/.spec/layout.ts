import { defineLayout } from '@astrale-os/spec/authoring'

export default defineLayout({
  entries: ['__tests__/', 'ca-fetch.ts', 'credential.ts', 'index.ts', 'session.ts', 'target.ts'],
  exact: true,
})
