import { defineLayout } from '@astrale-os/spec/authoring'

export default defineLayout({
  entries: [
    '__tests__/',
    'auth.ts',
    'call.ts',
    'command.ts',
    'credential.ts',
    'errors.ts',
    'index.ts',
    'self.ts',
    'session.ts',
    'target.ts',
    'tsconfig.json',
  ],
  exact: true,
})
