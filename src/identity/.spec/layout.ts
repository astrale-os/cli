import { defineLayout } from '@astrale-os/spec/authoring'

export default defineLayout({
  entries: [
    '__tests__/',
    'index.ts',
    'registration.ts',
    'registry.ts',
    'transfer.ts',
    'tsconfig.json',
  ],
  exact: true,
})
