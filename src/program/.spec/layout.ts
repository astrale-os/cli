import { defineLayout } from '@astrale-os/spec/authoring'

export default defineLayout({
  entries: ['__tests__/', 'build.ts', 'command.ts', 'index.ts', 'options.ts', 'registry.ts'],
  exact: true,
})
