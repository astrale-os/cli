import { defineLayout } from '@astrale-os/spec/authoring'

export default defineLayout({
  entries: ['__tests__/', 'files.ts', 'identities.ts', 'index.ts', 'paths.ts', 'tsconfig.json'],
  exact: true,
})
