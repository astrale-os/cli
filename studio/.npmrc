# Studio resolves @astrale-os from public npm. Shell 0.3.8-beta.1 is unpublished,
# so package.json pins the in-repo vendor tarball for `cd studio && bun install`.
@astrale-os:registry=https://registry.npmjs.org
