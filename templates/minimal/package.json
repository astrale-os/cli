{
  "name": "@astrale-os/astrale-domain-domain",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "typecheck": "bunx tsgo --noEmit",
    "test": "bunx vitest run --config test/vitest.config.ts"
  },
  "dependencies": {
    "@astrale-os/kernel-core": ">=0.3.0 <1.0.0",
    "@astrale-os/kernel-dsl": ">=0.1.0 <1.0.0",
    "@astrale-os/kernel-host": ">=0.5.0 <1.0.0",
    "@astrale-os/kernel-runtime": ">=0.3.0 <1.0.0",
    "@astrale-os/kernel-test": ">=0.1.0 <1.0.0",
    "@astrale-os/sdk": ">=0.1.0 <1.0.0",
    "zod": "^4.3.6"
  },
  "devDependencies": {
    "vitest": "^3.2.4",
    "wrangler": "^4.0.0"
  }
}
